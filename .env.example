# Shared environment variables for docker-compose
# Copy this file to .env and fill in your actual values

# Better Auth Secret (MUST be the same for frontend and backend)
# Generate a secure random string (min 32 characters)
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long-please-change-this

# Database URL
# Use local PostgreSQL (via docker-compose)
DATABASE_URL=postgresql://todouser:todopass@postgres:5432/tododb

# Or use Neon Serverless PostgreSQL
# DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
